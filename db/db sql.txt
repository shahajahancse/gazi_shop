ALTER TABLE `db_items` ADD `box_qty` INT NOT NULL DEFAULT '1' AFTER `unit_id`;
ALTER TABLE `db_purchaseitems` ADD `box_qty` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `item_id`, ADD `box_per_qty` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `box_qty`, ADD `pieces` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `box_per_qty`;
ALTER TABLE `db_purchaseitems` ADD `profit_total` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `profit_margin_per`;
ALTER TABLE `db_sales` ADD `profit_total` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `grand_total`;
ALTER TABLE `db_salesitems` ADD `sales_box` INT NOT NULL DEFAULT '0' AFTER `item_id`, ADD `sales_pieces` INT NOT NULL DEFAULT '0' AFTER `sales_box`;
ALTER TABLE `db_salesitems` ADD `purchase_price` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `total_cost`, ADD `profit_per` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `purchase_price`, ADD `tot_profit` DECIMAL(12,2) NOT NULL DEFAULT '0' AFTER `profit_per`;